import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://franco2444.github.io',
  // If the repo is NOT named "francojoselevich.github.io", uncomment and set:
  // base: '/portfolio',
});
